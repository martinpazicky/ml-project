1. Install pip
2. Install python (version 3.10 recommended)
3. Open the jupyter notebook (bank-churners.ipynb) and install
the required libraries using "pip install -r requirements.txt" into your virtual environment
4. Execute all the cells in the jupyter notebook file (.ipynb)
